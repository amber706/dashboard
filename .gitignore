node_modules
dist
build
.DS_Store
.env
.env.local
.env.*.local
*.log
.vite

# Supabase CLI scratch (linked-project.json, cli-latest)
supabase/.temp/

# Auto-loaded skill plugins / lockfile — local-only, not project source.
# Prevents `git add -A` from sweeping these into reporting commits.
.agents/
.claude/
skills-lock.json
